"""Bottom-nav apps + sub_menus for Senat-Digit FS (S2S).

Filled during §3.5 feature module work. At clone time returns an empty list
so the seed pipeline doesn't try to create application records yet.
"""


def get_senat_digit_fs_seed_app():
    """Return the application/sub_menu tree for Senat-Digit FS (S2S).

    Returns an empty list during clone-and-rename phase.
    """
    return []
